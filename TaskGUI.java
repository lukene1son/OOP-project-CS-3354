package todo;

import javax.swing.*;

public class TaskGUI extends JFrame {

    public TaskGUI() {
        setTitle("To-Do List");
        setSize(400, 300);
        setDefaultCloseOperation(EXIT_ON_CLOSE);

        // placeholder GUI window only
        setVisible(true);
    }
}
